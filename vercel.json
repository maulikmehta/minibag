{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm install --prefix packages/minibag --include=dev && cd packages/minibag && npm run build",
  "outputDirectory": "packages/minibag/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
